import 'package:flutter/material.dart';

class AppColors {
  // Light theme colors
  static const Color lightPrimary = Colors.blue;
  static const Color lightBackground = Color(0xFFF5F5F5); // Colors.grey[50]
  static const Color lightCardBackground = Colors.white;
  static const Color lightTextPrimary = Colors.black87;
  static const Color lightTextSecondary = Color(0xFF757575); // Colors.grey[600]
  static const Color lightBorder = Color(0xFFE0E0E0); // Colors.grey[300]

  // Dark theme colors
  static const Color darkPrimary = Colors.blue;
  static const Color darkBackground = Color(0xFF121212);
  static const Color darkCardBackground = Color(0xFF1E1E1E);
  static const Color darkTextPrimary = Colors.white;
  static const Color darkTextSecondary = Color(0xFFB0B0B0);
  static const Color darkBorder = Color(0xFF333333);

  static const Color calories = Colors.orange;
  static const Color protein = Colors.lightBlue;
  static const Color carbs = Colors.green;
  static const Color fat = Colors.amber;

  // Status colors
  static const Color success = Colors.green;
  static const Color warning = Colors.orange;
  static const Color error = Colors.red;
}
